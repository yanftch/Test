<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="keyboard_style">
        <item name="android:textSize">12sp</item>
        <item name="android:gravity">center</item>
        <item name="android:background">@drawable/keyboard_background</item>
        <item name="android:textColor">#353535</item>
    </style>
    <!-- dialog 样式  activity变暗 -->
    <style name="dialog_background_dimEnabled" parent="@android:style/Theme.Dialog">
        <item name="android:backgroundDimEnabled">true</item>
        <!-- activity 变暗 dd-->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsTranslucent">true</item>
        <!-- 半透明 -->
        <item name="android:textColor">#242424</item>
    </style>

    <declare-styleable name="pwdNoView">
        <attr name="length" format="integer"/>
        <attr name="borderColor" format="color"/>
        <attr name="borderWidth" format="dimension"/>
        <attr name="myDividerColor" format="color"/>
        <attr name="dividerWidth" format="dimension"/>
        <attr name="code" format="string"/>
        <attr name="codeColor" format="color"/>
        <attr name="pointRadius" format="dimension"/>
        <attr name="myTextSize" format="dimension"/>
        <attr name="showType" format="enum">
            <enum name="word" value="1"/>
            <enum name="password" value="2"/>
        </attr>
    </declare-styleable>
    <!--Dialog进出动画-->
    <style name="dialogWindowAnim" mce_bogus="1" parent="android:Animation">
        <item name="android:windowEnterAnimation">@anim/dialog_enter_anim</item>
        <item name="android:windowExitAnimation">@anim/dialog_exit_anim</item>
    </style>

    <style name="loadingProgressBar">
        <item name="android:indeterminateDrawable">@drawable/progress</item>
        <item name="android:minWidth">@dimen/progress_w_h</item>
        <item name="android:maxWidth">@dimen/progress_w_h</item>
        <item name="android:minHeight">@dimen/progress_w_h</item>
        <item name="android:maxHeight">@dimen/progress_w_h</item>
    </style>

    <style name="dialog_style">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:backgroundDimEnabled">false</item>
    </style>
    <style name="dialogstyle">
        <!--设置dialog的背景-->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!--设置Dialog的windowFrame框为无-->
        <item name="android:windowFrame">@null</item>
        <!--设置无标题-->
        <item name="android:windowNoTitle">true</item>
        <!--是否浮现在activity之上-->
        <item name="android:windowIsFloating">true</item>
        <!--是否半透明-->
        <item name="android:windowIsTranslucent">true</item>
        <!--设置窗口内容不覆盖-->
        <item name="android:windowContentOverlay">@null</item>
        <!--设置动画，在这里使用让它继承系统的Animation.Dialog-->
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <!--背景是否模糊显示-->
        <item name="android:backgroundDimEnabled">true</item>
    </style>
</resources>
